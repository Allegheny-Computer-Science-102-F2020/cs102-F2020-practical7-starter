"""Compute the Cartesian product of two sets."""

from typing import Iterator
from typing import Set


def cartesian_product(first: Set[int], second: Set[int]) -> Iterator[Set[int]]:
    """Compute and return the Cartesian product of two sets."""
    # TODO: iterate through all of the elements in the first set
    # TODO: iterator through all of the elements in the second set
    # TODO: create a set that contains those elements and yield it
